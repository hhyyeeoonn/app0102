public class Tank extends Unit {
	
	public static void main(String[] args) {
		Tank t1 = new Tank();
				t,work();
	}
	// 구현부가 없는 메서드를  추상메서드라고 한다
	public abstract void work() {}
	// 구현부가 없다면 추상메소드, 추상클래스의 역할 : 상속 
	@Override
		System.out.println(this.name + "싸우다");
	
	Public satatic void main(String[] srgs) {
		
		// 1) 추상클래스는 객체를 만들 수 없다
		// -> 추상클래스는 추상메소드를 가진다
		// -> 추상메소드는 메소드의 구현부가 없다
		Unit u = new Unit();
	}
}
}
